# Frontend Console

The frontend (`/frontend`) provides a lightweight Stable Suite Console with:

- deploy/configure actions
- regime readout
- demo mode action runners
- claimable reward + claim action wiring

It is designed as an operational control panel for demo/judging workflows.
